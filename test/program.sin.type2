// sin: 12,27,140,12,28,12,11,12,24,143,12,24,12,27,144,12,28,12,11,12,12,143,12,24,11

  // => [x, (1-x)]
  dup, 1, exch, sub

  // => [4x(1-x)]
  mul, 4, mul

  // => [4x(1-x), 5 - 4x(1-x)]
  dup, 5, exch, sub

  // => [4x(1-x) / 5 - 4x(1-x)]
  div

  // => [4 * ...]
  4, mul

return